// Name these taks the way you want
// Configure command in make to use desired amount of threads (now 4)
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "STflash",
            "group": "build",
            "type": "shell",
            "command": "make",
            "args": ["flash"]
        }
    ]
}